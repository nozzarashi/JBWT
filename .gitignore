# Зависимости — они ставятся командой npm install, в репо им не место (тяжёлые)
node_modules/

# Секреты — токен бота и chat_id. НИКОГДА не должны попасть на GitHub
.env

# Логи и временный мусор
*.log
debug-*.png
debug-*.html

# Локальные настройки IDE/инструментов
.claude/
